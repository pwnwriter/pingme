[project]
name = "pingme"
version = "0.1.0"
description = "Scan for nearby hackathons and CTFs, notify via Discord"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "pyyaml>=6.0",
    "selectolax>=0.3",
]
